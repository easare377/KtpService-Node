{
  "name": "easare-ktp-service",
  "version": "1.0.0",
  "description": "Custom socket protocol for real-time communication.",
  "main": "dist/main.js",
  "types": "dist/main.d.ts",
  "scripts": {
    "prepublish": "npm run build",
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Emmanuel Asare",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^4.6.4"
  }
}